---
name: "Lint"
on:  # yamllint disable-line rule:truthy
  push:
    branches:
      - "!dependabot/*"
      - "main"
  pull_request:
    branches: ["*"]
  merge_group:
    types:
      - "checks_requested"
jobs:
  go-license-check:
    name: "License Check"
    runs-on: "depot-ubuntu-24.04-small"
    steps:
      - uses: "actions/checkout@v4"
      - uses: "authzed/actions/setup-go@main"
      - name: "Check Licenses"
        uses: "authzed/actions/go-license-check@main"
        with:
          ignore: "buf.build"  # Has no license information

  go-lint:
    name: "Lint Go"
    runs-on: "depot-ubuntu-24.04"
    steps:
      - uses: "actions/checkout@v4"
      - uses: "authzed/actions/setup-go@main"
      # NOTE: this installs golangci-lint. It also runs golangci-lint,
      # which is also run by lint:go. This is a little duplication of work,
      # but shouldn't be a huge cost because the results are cached.
      - uses: "authzed/actions/golangci-lint@golangci-lint-v2"
      - name: "Lint Go"
        run: "go run mage.go lint:go"
      - uses: "chainguard-dev/actions/nodiff@main"
        with:
          path: ""
          fixup-command: "go run mage.go lint:go"

  extra-lint:
    name: "Lint YAML & Markdown"
    runs-on: "depot-ubuntu-24.04-small"
    steps:
      - uses: "actions/checkout@v4"
      - uses: "authzed/actions/setup-go@main"
      - name: "Lint Everything Else"
        run: "go run mage.go lint:extra"
      - uses: "chainguard-dev/actions/nodiff@main"
        with:
          path: ""
          fixup-command: "go run mage.go lint:extra"
